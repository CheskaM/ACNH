
/* can't use because we don't have a proptypes folder

import PropTypes from 'prop-types'

function Villager(props){
    return(
        <div className ="villager">
            <p>Name: {props.name}</p>
            <p>Birthday Day: {props.birthday_day}</p>
            <p>Islander: {props.islander ? "Yes" : "No"}</p>
        </div>
    );
}
Villager.propTypes = {
    name: PropTypes.string,
    age: PropTypes.number,
    isVillager: PropTypes.bool,
}
export default Villager */

/*function Villager(props){

    if(props.isClicked){
        return <h2>Click to meet your Villager {props.VillagerInfo}</h2>
    }
    else{
        return <h2>Click Me!!!</h2>
    }
}

export default Villager*/